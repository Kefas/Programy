#include <iostream>
#include <time.h>
#include <stdlib.h>

using namespace std;

int main(int argv, char *argc[])
{
  int tablica[10],seed;
  seed=time(NULL);
  srand(seed);

  for(int i=0;i<10;i++) tablica[i]=rand();

  cout << "Sortowanie babelkowe" << endl;
